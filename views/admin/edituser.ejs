<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit user</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
<body style="background-color: rgb(255, 255, 255);">
    <div class="container text-center mt-5">
        <h2 class="mt-5 mb-5">Welcome to the edit panel</h2>
    </div>
    <div class="container">
        <form action="/admin/edituser/<%=user._id%>" method="post">
            <% if (err) { %>
                <p class="text-danger text-center "><%= err %></p>
            <% } %>
            <table class="table table-bordered table-hover border-dark">
                <thead class="text-dark text-center">
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="text-dark">
                            <div class="form-group">
                                <input type="text" name="UserName" value="<%=user.UserName%>" id="username" required class="form-control">
                            </div>
                        </td>
                        <td>
                            <div class="form-group">
                                <input type="email" name="Email" value="<%=user.Email%>" id="email" required class="form-control">
                            </div>
                        </td>
                        <td>
                            <div class="form-group text-center">
                                <button type="submit" class="btn btn-primary">Update</button>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </form>
    </div>
</body>
</html>